using UnityEngine;
using UnityEngine.Events;

namespace Assets.Scripts.View
{
    //TODO fort offset like f(transform) {get offset from triangle}
    class FortOffset : MonoBehaviour
    {
        [SerializeField] private Transform UpFortPoint;
        [SerializeField] private Transform DownFortPoint;        
    }
}
